apiVersion: v1
kind: Service
metadata:
  name: {{ include "vcs-agent.fullname" . }}
  labels:
    {{- include "vcs-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vcs-agent.selectorLabels" . | nindent 4 }}
